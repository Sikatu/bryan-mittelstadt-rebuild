import type { SiteAssets } from '@/types';

/**
 * Central image paths. Placeholder paths remain explicit so the readiness audit
 * can report every launch asset that still needs replacement.
 */
export const siteAssets: SiteAssets = {
  heroImage: '/images/bryan/hero/hero-placeholder.svg',
  reelPosterImage: '/images/placeholders/reel-poster.svg',
  portraitImage: '/images/placeholders/project-1.svg',
  verificationId: 'asset.hero-and-portfolio-images',
};
