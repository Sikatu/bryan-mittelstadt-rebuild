# Generated Reports

Launch and release reports are generated into ignored `.launch/` and `.release/` directories. They contain the current Git commit, environment configuration, blocker counts, and QA results. Do not treat a stale report as approval for a newer commit.
